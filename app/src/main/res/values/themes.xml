<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Calculadora" parent="Theme.MaterialComponents">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorSecondary">@color/primaryTextColor</item>
        <item name="colorAccent">@color/secondaryColor</item>
    </style>

    <style name="Theme.Calculadora" parent="Base.Theme.Calculadora" />

    <style name="CustomButton" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="strokeColor">@color/secondaryColor</item>
        <item name="strokeWidth">3dp</item>
        <item name="android:textColor">@color/secondaryColor</item>
    </style>

    <style name="CustomNumbers" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="strokeColor">@color/secondaryTextColor</item>
        <item name="strokeWidth">3dp</item>
        <item name="android:textColor">@color/secondaryLightColor</item>
    </style>
</resources>